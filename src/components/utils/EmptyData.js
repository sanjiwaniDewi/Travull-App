export default function EmptyData({ heigh }) {
    return (
        <div
            className={`${heigh} rounded-xl bg-slate-200 flex justify-center items-center`}
        >
            <p>tidak ada data</p>
        </div>
    );
}
