//

export default function LoginLayout({ children }) {
    return (
        <main className="flex relative w-full h-full loginBackground bg-opacity-50 ">
            {children}
        </main>
    );
}
